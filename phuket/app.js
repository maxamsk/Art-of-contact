const dialog=document.querySelector('dialog');document.querySelectorAll('[data-donate]').forEach(a=>a.addEventListener('click',e=>{if(!dialog.showModal)return;e.preventDefault();const frame=dialog.querySelector('iframe');if(!frame.src)frame.src=frame.dataset.src;dialog.showModal();document.body.style.overflow='hidden';}));dialog.querySelector('.close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close();});dialog.addEventListener('close',()=>document.body.style.overflow='');document.querySelectorAll('.toc a').forEach(a=>a.addEventListener('click',()=>a.closest('details').open=false));
window.addEventListener('message',e=>{if(e.origin===location.origin&&e.source===dialog.querySelector('iframe').contentWindow&&e.data==='close-payments')dialog.close();});